<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main_gradient">

    <!-- Full-screen Fragment Container -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Modern Floating Pill Navigation Bar - Added Scroll Behavior -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/nav_card_container"
        android:layout_width="@dimen/bottom_nav_width"
        android:layout_height="@dimen/bottom_nav_height"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="@dimen/bottom_nav_margin_bottom"
        app:cardCornerRadius="32dp"
        app:cardElevation="12dp"
        app:cardBackgroundColor="@color/color_bottom_nav_bg"
        app:strokeColor="@color/color_bottom_nav_stroke"
        app:strokeWidth="1dp"
        app:layout_behavior="com.google.android.material.behavior.HideBottomViewOnScrollBehavior"
        android:outlineAmbientShadowColor="#20000000"
        android:outlineSpotShadowColor="#30000000">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            app:elevation="0dp"
            app:labelVisibilityMode="unlabeled"
            app:itemIconSize="@dimen/bottom_nav_icon_size"
            app:itemIconTint="@color/bottom_nav_color_selector"
            app:itemBackground="@android:color/transparent"
            app:itemRippleColor="#1A000000"
            app:menu="@menu/bottom_nav_menu" />

    </com.google.android.material.card.MaterialCardView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>