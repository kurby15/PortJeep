<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_background_primary"> <!-- Added background reference -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_page_schedule_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingStart="20dp"
        android:paddingTop="4dp"
        android:paddingEnd="20dp"
        android:paddingBottom="160dp"
        tools:visibility="visible" />

    <LinearLayout
        android:id="@+id/layout_page_empty_state"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="-55dp"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="128dp"
            android:layout_height="128dp"
            android:layout_marginBottom="12dp"
            android:contentDescription="No schedules"
            android:src="@drawable/no_schedule"
            app:tint="@color/color_text_muted" />

        <TextView
            android:id="@+id/tv_page_empty_state"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="All schedules are currently assigned."
            android:textColor="@color/color_text_muted"
            android:textSize="14sp" />

    </LinearLayout>

</FrameLayout>